
ng new <name of the app>
ng serve
--install Bootstrap v3
  $ npm install --save bootstrap@3
-- create component
  $ ng generate component servers 
  OR 
  $ ng g c servers

-- create a service
  $ ng g service services/auth
  optionally, we can skip the creation of spec files
  $ ng g service services/auth --spec=false

